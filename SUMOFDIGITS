#include <stdio.h>
void main() {
    int n,sum=0;
    printf("Enter a number: ");
    scanf("%d", &n);
    while (n!= 0) {
        int a = n%10;  
        sum += a;      
        n /= 10; 
    }
    printf("Sum of digits: %d\n", sum);
}
